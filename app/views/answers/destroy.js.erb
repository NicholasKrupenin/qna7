$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if @answer.destroyed? %>
  $('.answers [data-answer-id="#{@answer.id}"]').remove()
  $('.notice').html('Your question successfully delete.')
<% end %>
