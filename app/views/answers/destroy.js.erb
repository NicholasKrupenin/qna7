$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if @answer.destroyed? %>
  $('.answers [data-answer="#{@answer.id}"]').remove()
  $('.answers').html('<%= j render 'answers/star' %>')
  $('.notice').html('Your answer successfully delete.')
<% end %>
